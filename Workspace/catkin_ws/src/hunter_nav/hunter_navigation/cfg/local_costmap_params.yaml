local_costmap:
  global_frame: map
  robot_base_frame: base_link
  update_frequency: 4
  publish_frequency: 10.0
  static_map: false
  rolling_window: true
  width: 7.0
  height: 7.0
  resolution: 0.05
  transform_tolerance: 1.0
  

#   observation_sources: laser_scan_sensor
#   laser_scan_sensor:
#     { data_type: LaserScan, topic: scan, marking: true, clearing: true }
#    observation_sources: rslidar_sensor
#    rslidar_sensor:
#      { data_type: PointCloud2, topic: points2, marking: true, clearing: true }

#  inflation_layer:
#    enabled: true
#    cost_scaling_factor: 2.58 #10.0 # exponential rate at which the obstacle cost drops off (default: 10)
#    inflation_radius: 1.75 #0.05 # max. distance from an obstacle at which costs are incurred for planning paths.

  plugins:
#   - {name: static_layer,        type: "costmap_2d::StaticLayer"}
   - {name: obstacle_layer,      type: "costmap_2d::ObstacleLayer"}
#   - {name: inflation_layer,         type: "costmap_2d::InflationLayer"}
#   - {name: ultrasonic0, type: "range_sensor_layer::RangeSensorLayer"}